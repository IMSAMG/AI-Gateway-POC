# AI-Gateway-POC
Instead of every developer worrying about prompts, context windows, token limits, and costs, the platform automatically applies optimization techniques before sending requests to the model.
